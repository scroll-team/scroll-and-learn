/**
 * Auth service layer.
 *
 * Encapsulates all authentication operations:
 * - Sign up / sign in / sign out
 * - Session management
 * - Profile CRUD
 *
 * Implemented in Step 3 (Supabase Setup & Authentication).
 */

export {};
