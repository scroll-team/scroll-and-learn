/**
 * Document service layer.
 *
 * Encapsulates all document-related operations:
 * - Upload PDF to Supabase Storage
 * - CRUD operations on the documents table
 * - Trigger AI processing pipeline
 *
 * Implemented in Step 4 (PDF Upload & Document Library).
 */

export {};
