/**
 * Quiz service layer.
 *
 * Encapsulates all quiz-related operations:
 * - Fetch quizzes for a document
 * - Submit quiz attempts
 * - Calculate scores and XP
 *
 * Implemented in Step 6 (Interactive Quiz Experience).
 */

export {};
